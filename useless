ltl e1: [] (((cs0+cs1)<=1))
ltl e2: [] ((! ((try0==1))) || (<> ((cs0==1))))
ltl e3: [] ((! ((try1==1))) || (<> ((cs1==1))))
  the model contains 3 never claims: e3, e2, e1
  only one claim is used in a verification run
  choose which one with ./pan -a -N name (defaults to -N e1)
  or use e.g.: spin -search -ltl e1 /Users/zhuang/workspace-gp/testSwig2/output/mutex.pml
